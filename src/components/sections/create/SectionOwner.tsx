import type { FC } from 'react';

// TODO: コンポ名の変更
const SectionOwner: FC = () => {
	return (
		<section className="">
			<p>チーム情報はこちら</p>
			{/* TODO: チーム・主催者情報の設定用フォームの作成 */}
		</section>
	);
};

export default SectionOwner;
